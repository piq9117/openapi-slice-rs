[package]
name = "openapi-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["derive"]}
serde_yaml = "0.9.34"

[dev-dependencies]
insta = { version = "1.46.3", features = ["yaml"]}
glob = "0.3.3"
